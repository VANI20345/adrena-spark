// Default values for the application
// No default city filter - users see all locations
export const DEFAULT_CITY = null;

// Referral reward amount in SAR
export const REFERRAL_REWARD_AMOUNT = 10;

// Shield member badge for first 1000 beta users
export const SHIELD_MEMBER_LIMIT = 1000;
